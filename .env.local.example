# Vercel Postgres — copy from: Vercel Dashboard → Storage → Postgres → .env.local tab
POSTGRES_URL=

# Vercel Blob — copy from: Vercel Dashboard → Storage → Blob → .env.local tab
BLOB_READ_WRITE_TOKEN=
